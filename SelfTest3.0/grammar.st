~ 标题部分 ~
    一些说明等内容...

# 第一个分类了
一些分类说明，可以不写

命令就放在这里了
    1 > 表示第一个参数，然后这里是参数的介绍
    这里是命令的说明

继续这样写
    1 int> 参数就像这样
    2 int> 什么情况尽量要先想到
    int< 这样介绍返回值
    介绍可以换行，这些内容的顺序可以无所谓，但是必须分好组
    @指定一下和谁相似
    ```
    代码示例区域
    ```

## 二级分类
如果没有指定上级分类，那么这个分类则会跟随上一层为子类
如果分类层级不正确，比如上面的是二级分类，无法直接拉过来一个第四级分类作为子类

### 分类, 一个 ~ 表示一级分类
分类下直接换行表示分类的介绍, 可以不用写

# lal

换两行开始, 写一个函数, 或者一个要被说明的东西
    参数1 类型> 这样来介绍这个函数的参数, 包括类型等
    参数2 类型> 如果没有参数, 这个地方可以省略
    类型< 如果这个函数有返回值, 用大于号指向一个类型
    然后这里是详细的介绍,可以换行等. 没有的东西可以省略

    @如果这个函数和其他函数有些关联,可以用这个符号指定
    ```
    用md中的代码段来表示代码实例
    ```